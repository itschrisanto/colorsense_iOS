# App Store screenshot set

Captured September 6, 2026 on the iPhone 17 Pro Max simulator (iOS 26.5), light appearance, standard text size. All six PNGs are 1320 × 2868. Open `index.html` to review them together.

1. Palette bands
2. Contrast checker
3. Palette Health score
4. Visualizer
5. SVG Recolor
6. Schemes

The palette is the app's existing `ExtractedPalette.sample`. SVG artwork is an original geometric fixture made for this capture; it contains no customer data or third-party marks. The temporary capture routes selected existing tool screens and enabled their Pro presentation locally. Those routes were removed from the app source after capture. No backend entitlement or customer data was changed.

These are native screenshots, without added promotional claims. StoreKit is still a release blocker. Review this set against the final submitted build and replace any affected capture before upload. A photo extraction screenshot can be added later using an approved sample photo; it is not included in this six-image set.
